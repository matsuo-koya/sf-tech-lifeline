# コントリビューションガイド

年表への項目追加を歓迎します。以下のいずれかの方法で貢献できます。

## 方法1: アプリ内フォームから提案（最も簡単）

アプリの「項目を提案」ボタンから、入力フォームに記入して GitHub Issue を作成できます。
Issue を作成したら、`item-proposal` ラベルを付けてください（メンテナーが確認後、自動的に PR が生成されます）。

複数項目を一度に提案することも可能です。

## 方法2: GitHub Issue から提案

[Issue 作成ページ](https://github.com/matsuo-koya/sf-tech-lifeline/issues/new)から、項目提案テンプレートを使って Issue を作成してください。

### バッチ提案（複数項目の一括投稿）

1つの Issue に複数項目をまとめて書けます。AI ツールで生成したデータを貼り付けるのにも便利です。

```markdown
### 1
- 年: 2007
- タイトル: 初音ミク発売
- カテゴリ: music
- 分野: boom
- アイコン: music
- 解説: クリプトン・フューチャー・メディアが発売した音声合成ソフトウェア...
- リンク: 初音ミク
- 検索クエリ: 初音ミク 2007

### 2
- 年: 2008
- タイトル: iPhone 3G 日本上陸
- カテゴリ: tech
- 分野: mobile
- アイコン: phone
- 解説: ...
- リンク: iPhone 3G
```

`### 数字` で区切ると、それぞれが1項目として認識されます。

## 方法3: 直接 PR を作成

`src/events.json` に1行（1オブジェクト）追加して Pull Request を作成してください。

### データスキーマ

```json
{
  "y": 2007,
  "t": "初音ミク発売",
  "cat": "music",
  "s": "boom",
  "ic": "music",
  "n": "クリプトン・フューチャー・メディアが発売した音声合成ソフトウェア...",
  "l": ["初音ミク"],
  "q": "初音ミク 2007",
  "a": "2026-08-11"
}
```

| フィールド | 必須 | 説明 |
|-----------|------|------|
| `y` | ✅ | 年（数値） |
| `t` | ✅ | タイトル（簡潔に） |
| `cat` | ✅ | カテゴリ: `"sf"` / `"tech"` / `"music"` |
| `s` | tech必須 | 分野キー（下表参照）。SF作品には不要 |
| `ic` | ✅ | アイコン名（下表参照） |
| `n` | ✅ | 解説テキスト（現代技術とのつながりを含む） |
| `l` | 推奨 | Wikipedia記事名の配列。`en:`付けると英語版。`"https://URL\|表示名"`で外部リンクも可 |
| `q` | 任意 | Amazon/YouTube検索クエリ |
| `a` | 任意 | 追加日（`"YYYY-MM-DD"`形式）。最新日付の項目が「最近の追加」に表示される |

### 分野キー（`s`）

| キー | 分野 |
|------|------|
| `pc` | コンピューター |
| `ai` | AI |
| `net` | ネット・インフラ |
| `sns` | SNS・ネット文化 |
| `mobile` | モバイル・通信 |
| `game` | ゲーム |
| `inst` | 楽器・DTM |
| `audio` | オーディオ |
| `video` | 映像・カメラ・CG |
| `wear` | ウェアラブル |
| `tablet` | タブレット・電子書籍 |
| `xr` | VR・AR |
| `robot` | ロボット |
| `space` | 宇宙・測位 |
| `quantum` | 量子 |
| `boom` | ブーム・社会現象 |
| `other` | その他 |
| `artist_w` | 洋楽アーティスト |
| `artist_j` | 邦楽アーティスト |

### アイコン名（`ic`）

`chip` / `computer` / `ai` / `book` / `rocket` / `satellite` / `phone` / `wifi` / `globe` / `gamepad` / `music` / `video` / `film` / `tv` / `watch` / `robot` / `camera` 等（`src/App.jsx` の `PATHS` 定義を参照）

## 注意事項

- 解説（`n`）は事実に基づき、現代技術とのつながりを含めてください
- Wikipediaリンク（`l`）は実際に存在する記事名を指定してください
- 年代順に挿入する必要はありません（自動的にソートされます）
- 同一の出来事が既に存在しないか、まず検索で確認してください

## トピックの「糸」の追加

`src/threads.json` に追加できます：

```json
{
  "key": "my-thread",
  "title": "糸のタイトル",
  "lead": "リード文",
  "items": [
    { "y": 2007, "m": "初音ミク" }
  ]
}
```

`m` は項目タイトルに含まれる文字列で該当項目を引き当てます（見つからない場合は黙って除外されます）。

## ライセンス

貢献された内容は MIT ライセンスの下で公開されます。
